import {
  CheckoutConfirmationRoute,
  confirmationMetadata,
} from "@/features/delivery-matching/components/checkout-confirmation-route";

export const metadata = confirmationMetadata;
export default CheckoutConfirmationRoute;
