# Application URLs
NEXT_PUBLIC_APP_URL=http://localhost:3000
ADMIN_APP_URL=http://localhost:3000
NEXT_PUBLIC_STOREFRONT_URL=http://localhost:3001
# Optional public CDN logo; local /images/mobgreen.png remains the fallback
NEXT_PUBLIC_BRAND_LOGO_URL=https://res.cloudinary.com/your-cloud-name/image/upload/mob-greens/brand/mobgreen.png

# PostgreSQL (use Railway's private DATABASE_URL in production)
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DATABASE

# Admin sessions — generate a random value of at least 32 characters
SESSION_SECRET=replace-with-a-random-secret

# One-time administrator bootstrap — remove after creating the first owner
ADMIN_BOOTSTRAP_EMAIL=owner@example.com
ADMIN_BOOTSTRAP_NAME=Store Owner
ADMIN_BOOTSTRAP_PASSWORD=replace-with-a-strong-password

# Cloudinary
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret

# Mapbox
NEXT_PUBLIC_MAPBOX_ACCESS_TOKEN=your-restricted-public-token
MAPBOX_ACCESS_TOKEN=your-private-server-token

# Order notifications — use Resend's HTTPS API in Railway (recommended)
# Verify the sender/domain in Resend before enabling this in production.
RESEND_API_KEY=re_your-sending-access-key
ORDER_NOTIFICATION_TO=store@example.com
ORDER_NOTIFICATION_FROM=store@example.com

# Gmail SMTP order notifications — supported for local development or Railway Pro
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=store@example.com
SMTP_APP_PASSWORD=your-google-app-password

# Bitcoin checkout — keep false until NOWPayments API and IPN setup is verified
BITCOIN_CHECKOUT_ENABLED=false
BTC_RECEIVING_ADDRESS=your-public-bitcoin-address
NOWPAYMENTS_API_URL=https://api.nowpayments.io
NOWPAYMENTS_API_KEY=your-nowpayments-api-key
NOWPAYMENTS_IPN_SECRET=your-nowpayments-ipn-secret
